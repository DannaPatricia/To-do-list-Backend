# Configuración base compartida
# El perfil activo se define según el ambiente

# Configuración de JPA/Hibernate (común para todos los perfiles)
spring.jpa.open-in-view=false

# Por defecto usa el perfil 'dev' si no se especifica otro
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}